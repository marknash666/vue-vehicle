# safelist
branches:
  only:
      - /.*/

matrix:
  fast_finish: true
  include:
      - os: linux
        dist: xenial
        sudo: required

language: node_js
node_js:
  - "8"

install:
  - npm install

script:
  - npm run test

